# Deprecated v1 Registry

This directory is retained for historical provenance and legacy research tests only. It must **not** be used by the live demo. The patched API serves `artifacts/model_registry/v2`, which contains the fitted classifier, probability calibration, quantile severity models, CQR calibration, and SHAP-capable classifier.
