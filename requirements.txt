# Core dependencies for the patched demo / API / dashboard / verification path.
# For the complete historical research environment use: pip install -e ".[all]"
pyyaml>=6.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
catboost>=1.2
lightgbm>=4.0
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0
httpx>=0.24
streamlit>=1.25
plotly>=5.15
pytest>=7.4
pyarrow>=12.0
